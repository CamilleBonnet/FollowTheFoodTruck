<div class="container">
  <div class="row">
    <div class="col-xs-12 col-md-6 col-md-offset-3">
      <ul>
        <%= @user.first_name %>, here is a list of all your current orders
        <li>
          <% @user.baskets.each do |basket| %>
            <%= render 'shared/message_user_orders', basket: basket %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</div>
